# Copy this file to config.custom.yaml for local custom overrides.
# Keep config.yaml close to upstream to reduce merge conflicts.

app:
  timezone: Asia/Shanghai

ai:
  model: deepseek/deepseek-v4-flash

storage:
  backend: auto

  remote:
    endpoint_url: ""
    bucket_name: ""
    access_key_id: ""
    secret_access_key: ""
    region: ""
